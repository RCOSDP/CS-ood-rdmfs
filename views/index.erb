<h1><%= title %></h1>

Output generated: <%= Time.now %>

<table class="table table-bordered">
  <tr>
    <th>available actions</th>
    <th>MOUNT_PATH</th>
    <th>RDM_NODE_ID</th>
    <th>RDM_API_URL</th>
    <th>RDM_TOKEN</th>
  </tr>
  <% @data.each do |k, v| %>
  <tr>
    <form action="" method="post">
    <td>
        <input type="submit" name="act" value="<%= v['available_action'] %>">
        <input type="<%= v['available_action'] == 'mount' ? 'submit' : 'hidden' %>" name="<%= v['available_action'] == 'mount' ? 'act' : 'dummy' %>" value="delete">
    </td>
    <td><input type="text" name="mount_path" id="mount_path" value="<%= k %>"required></td>
    <td><input type="text" name="rdm_node_id" id="rdm_node_id" value="<%= v['rdm_node_id'] %>"required></td>
    <td><input type="text" name="rdm_api_url" id="rdm_api_url" value="<%= v['rdm_api_url'] %>"required></td>
    <td><input type="text" name="rdm_token" id="rdm_token" value="<%= v['rdm_token'] %>"required></td>
    </form>
  </tr>
  <% end %>
  <tr>
    <form action="" method="post">
    <td><input type="submit" name="act" value="mount"></td>
    <td><input type="text" name="mount_path" id="mount_path" required></td>
    <td><input type="text" name="rdm_node_id" id="rdm_node_id" required></td>
    <td><input type="text" name="rdm_api_url" id="rdm_api_url" required></td>
    <td><input type="text" name="rdm_token" id="rdm_token" required></td>
    </form>
  </tr>

<% @params %>

</table>
